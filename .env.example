# MongoDB connection (for Phase 2)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=research_aggregator

# PostgreSQL connection (for Phase 2)
POSTGRES_URI=postgresql://user:password@localhost:5432/research_aggregator

# Server
HOST=0.0.0.0
PORT=8000
